# Supabase Database Configuration
# Copy this file to .streamlit/secrets.toml and add your actual credentials

# Option 1: Database password (for direct PostgreSQL connection - RECOMMENDED)
# Find this in Supabase: Settings > Database > Connection string > Password
SUPABASE_DB_PASSWORD = "your_database_password_here"

# Option 2: API Key (if you prefer, but database password is better for direct connection)
# Note: API keys (sbp_...) are for REST API, not direct PostgreSQL connection
# SUPABASE_API_KEY = "sbp_..."

# Enable/disable Supabase (set to 'false' to use Excel only)
USE_SUPABASE = "true"
